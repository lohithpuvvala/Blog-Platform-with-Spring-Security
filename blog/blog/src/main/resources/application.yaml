spring:
  application:
    name: blog
# Database configuration for MySQL
  datasource:
    url: jdbc:mysql://localhost:3307/blogdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: changemeinprod!

# JPA and Hibernate configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: your-256-bit-secret-key-here-make-it-at-least-32-bytes-long